const table = document.getElementById('age-table');
const labels = table.getElementsByTagName('label');
const rowAge = table.rows[0].cells[0];
const form = document.getElementsByName('search')[0];
const inputFirst = form.getElementsByTagName('input')[0];
const inputs = form.querySelectorAll('input');
const inputLast = inputs[inputs.length - 1];
